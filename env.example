# AWS S3 Configuration
# Replace these values with your actual AWS credentials
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
AWS_S3_BUCKET_NAME=quran-competition-videos

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# MySQL Database Configuration
# Required for storing user registrations and video submissions
DATABASE_HOST=localhost
DATABASE_PORT=3306
DATABASE_NAME=quran_competition
DATABASE_USER=root
DATABASE_PASSWORD=your_password

# Email Configuration (Optional - for future implementation)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASSWORD=your_app_password

# Video Processing Configuration
# Videos are uploaded directly without processing

# Redis Configuration (Required for async video processing)
# For local development, install Redis:
#   Ubuntu/Debian: sudo apt-get install redis-server
#   macOS: brew install redis
#   Windows: Download from https://redis.io/download or use WSL
#   Or use a cloud Redis service (Redis Cloud, AWS ElastiCache, etc.)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
# Or use a Redis URL (for cloud Redis services):
# REDIS_URL=redis://:password@host:port

